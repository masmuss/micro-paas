pre-commit:
  parallel: true
  commands:
    fmt:
      glob: "*.go"
      run: gofmt -w {staged_files} && goimports -w {staged_files}

    linter:
      glob: "*.go"
      run: golangci-lint run ./...

    tidy:
      run: go mod tidy
